{
  "name": "discord-notifier",
  "version": "1.0.0",
  "description": "GitHub Action — Discord push notifications via Message Components v2",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "build": "bun build src/index.js --outdir dist --target node",
    "dev": "bun src/index.js"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  }
}